extends ../layout/default.pug 
include ../mixins/site-header.pug

block header 
    - const currentPage = 'room4';
    +mixinSiteHeader(currentPage) 

block content
    h1 Welcome to the Room 4
    .board 
        - for (var i = 0; i < 36; ++i)
            .board-item(id=i+1)

block pageScripts 
    script(src="../js/index.js", type="module")
    script(src="../js/game.js", type="module")